% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count.R
\name{count}
\alias{count}
\title{Count elements or occurrences in a vector}
\usage{
count(x, value = NULL, na.rm = TRUE)
}
\arguments{
\item{x}{An atomic vector (logical, integer, double, character, etc.).}

\item{value}{A single value to count in \code{x}. If \code{NULL}, counts non-missing elements.}

\item{na.rm}{Logical. If \code{TRUE} and \code{value} is supplied, missing values are ignored in the comparison.}
}
\value{
An integer scalar with the count.
}
\description{
Versatile counting helper:
\itemize{
\item If \code{value} is \code{NULL} (default), returns the number of \strong{non-missing} elements.
\item If \code{value} is provided, returns how many times \code{value} occurs in \code{x}.
}
}
\examples{
# Count non-missing elements
count(c(1, 2, NA, 4))           # 3

# Count occurrences of a value
count(c("a", "b", "a"), value = "a")  # 2

# Count logicals
count(c(TRUE, FALSE, TRUE), value = TRUE)  # 2

}
